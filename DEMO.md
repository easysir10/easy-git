# easy-git 插件演示文档

> 傻瓜式 git 新手助手 —— 完全不会 git、不会命令行的人，也能靠大白话对话完成所有 git 操作。

- 版本：0.2.3
- 仓库：https://github.com/easysir10/easy-git
- 适用：GitLab / GitHub / Gitee 等一切 git 托管平台
- 安装方法：见 [README.md](README.md)（两步：`dsh plugin --profile web add` + 在 `cordis.patch.yml` 登记）

---

## 一、它解决什么问题

小明是个**完全不懂 git 的小白**。以前听人说"你要 commit、push、rebase"，他一脸懵；
现在装了这个插件，他只需要**说人话**：

| 他心里想的 | 他开口说的 | 助手做的 |
| --- | --- | --- |
| 看看我的代码咋样了 | "看下 git 现状" | 体检：分支/身份/改动/冲突/同步状态 |
| 第一次用，不知道用哪个平台 | （自动被问）"你用的哪个平台？" | 记住平台，之后按它引导 |
| 想开始一个项目 | "把这个文件夹变成仓库" | 初始化 git 仓库 |
| 改完代码了 | "帮我提交" | 预览清单 → 提交 |
| 想传到服务器 | "推送" | 上传到远程仓库 |
| 同事改了代码 | "拉取" | 把新代码合并进来 |
| 出冲突了 | "有冲突" | 列出冲突 + 三选一引导解决 |

**全程没有一条命令行命令、没有一个文件夹路径需要他自己填。**

---

## 二、完整演示流程（小白从零到入门）

以下用"你说 → 助手回"的形式，完整走一遍。

### 第 1 步：第一次体检 + 选择平台

> 你说：**看下我 git 现状**

助手体检后回答：

```
✅ git 已经装好了（版本 2.45.1）。
但当前文件夹还不是一个 git 仓库。

接下来可以选：
1️⃣ 从GitHub/GitLab 等平台拉一个现成的项目 → 把仓库地址发给我，我用 git_beginner_pull 帮你下载。
2️⃣ 把当前文件夹变成仓库 → 告诉我，我用 git_beginner_setup 帮你初始化。

🔰 第一次使用 git？请先告诉我你用的是哪个平台：① GitHub　② GitLab　③ Gitee　④ 其他
（选好后告诉我，我会记住，之后所有步骤都按这个平台引导你）
```

> 你说：**我用 GitLab**

助手保存平台并给出 GitLab 专属指引：

```
✅ 已记住：你使用GitLab。之后所有 git 引导都会按GitLab的步骤来。

GitLab快速上手：
• 新建仓库：New project → Create blank project
• 获取克隆链接：点 Clone 按钮复制 HTTPS 链接
• 生成访问令牌：Settings → Access tokens（勾选 write_repository）

（如果以后换了平台，直接说"改成 GitLab"之类的话即可）
```

> 💡 如果你的仓库已经有远程地址，助手会**自动识别**平台（比如远程是 `github.com` 就自动按 GitHub 引导），不用手动选。

### 第 2 步：初始化配置

> 你说：**把这个文件夹变成仓库**

助手执行 `git init`（默认分支 main，与 GitLab/GitHub 一致）：

```
✅ 已把当前文件夹初始化为 git 仓库（默认分支 main）。
✅ 配置完成！现在可以正式使用 git 了：要拉代码就把仓库地址发我，要保存修改就告诉我"提交"。
```

> 💡 你的名字和邮箱如果在别处设置过，助手会直接复用；没设置的话，助手会问你"你的名字和邮箱是什么"，然后帮你写进全局配置。

### 第 3 步：写代码 + 提交

你写了一个 `README.md`，然后说：

> 你说：**帮我提交**

助手先**预览**要提交的文件，绝不盲提交：

```
✏️ 本次将提交 1 个文件：
   • 🆕 新增（还没加入 git）  README.md

提交 = 给这一批修改拍一张"快照"，方便以后回退、也方便上传到远程仓库。
请让用户用一句话说明改了什么（越简单越好，例如："修复了登录按钮点不动的问题"）
```

> 你说：**加了一个说明文件**

助手执行提交：

```
✅ 提交成功！

提交说明：加了一个说明文件
 README.md | 1 +
 1 file changed, 1 insertion(+)

下一步：把这次提交上传到远程仓库？告诉用户"推送"，我来帮你。
```

> 💡 不知道提交说明怎么写？告诉助手"你帮我起个名"，它会替你想一句再问你行不行。

### 第 4 步：绑定远程 + 推送

> 你说：**仓库地址是 https://gitlab.com/小明/demo.git，帮我绑上**

助手绑定远程地址（origin）。然后你说：

> 你说：**推送**

助手首次推送自动设置上游分支：

```
✅ 推送成功！你的代码已经上传到远程仓库，同事/合并请求里都能看到了。
```

> 💡 推送被拒绝（远程有别人新提交）？助手会说"先拉取把别人的代码合并进来，再重新推送"。
> 💡 认证失败？助手会按你的平台告诉你访问令牌在哪生成、怎么填。

### 第 5 步：拉取（同事改了代码）

> 你说：**拉取**

助手执行 `git pull`（合并方式，绝不改写你自己的提交）：

```
✅ 拉取完成！你已经拿到了别人最新提交的代码。
```

### 第 6 步：解决冲突（重点）

这次拉取，你和同事**改了同一个文件的同一处**——冲突了。助手先安慰你：

```
⚠️ 拉取时出现了冲突：你和别人的修改撞在了一起，git 不知道该保留哪个。

别担心，这很正常。请用 git_beginner_conflict 工具，我会一步步带你选择怎么处理。
```

然后列出冲突文件，给你三个选择：

```
⚠️ 有 1 个文件发生了冲突：
   • hello.txt

什么是冲突：你和同事都改了同一个文件的同一个地方，git 不知道听谁的，
就先把两个版本都留给你决定。

几种选择（先问用户选哪个，再告诉我）：
① 保留我的版本 —— 用你自己本地写的这一份（适合：这个文件主要是你写的）
② 保留对方的版本 —— 用别人/远程的那一份（适合：构建产物、锁文件、自动生成的文件）
③ 我手动改 —— 两个版本都要留一部分（推荐：普通代码，两边都改过重要内容）
   （手动改流程：先选 show 让我把冲突内容打开给你看 → 你改好 → 告诉我"改好了"）
```

> 你说：**② 保留对方的**（因为这是个锁文件）

```
✅ hello.txt 已保留对方的版本

🎉 所有冲突都解决好了！现在可以提交了：告诉用户"提交"，我会把这次合并保存成一个版本。
```

> 你说：**提交**

合并正式完成。冲突解决就是这么简单。

### 第 7 步：日常体检

> 你说：**体检**

```
📋 你当前 git 的情况（体检报告）：

• 当前分支：main
• 你的名字（提交时显示的作者名）：小明
• 你的邮箱（提交时显示的联系方式）：xiaoming@example.com
• 远程仓库：origin	https://gitlab.com/小明/demo.git (fetch)
• 引导平台：GitLab（可随时说"改成 xxx"来更换）
• 和远程对比：完全同步（本地和服务器上的代码一模一样）

✏️ 当前没有任何未提交的修改。
```

---

## 三、平台不一致自动提醒（v0.2.3 新增）

如果"引导平台"和"远程仓库"对不上（比如引导是 GitLab，远程却是 GitHub），体检时会自动提醒：

```
⚠️ 你的远程仓库看起来是 GitHub，但当前引导平台是 GitLab，两边对不上。
   要改成按 GitHub 引导？直接说"改成 GitHub"即可。
```

说一句"改成 GitHub"，立即切换，不用任何命令。

---

## 四、给小白的安全感设计（防呆）

| 场景 | 助手会怎么做 |
| --- | --- |
| 冲突还没解决 | 禁止提交 / 拉取 / 推送，先引导解决冲突 |
| 正在合并中 | 自动识别（MERGE_HEAD 等），提示"还差最后一步提交" |
| 提交缺名字/邮箱 | 友好提示，帮你设置后再提交，不甩英文报错 |
| 命令报错 | 技术报错助手自己看，**不会把吓人的报错丢给你** |
| 不知道该在哪执行 | 用你所在的工作区，**不用你填任何路径** |
| 专业词（分支/合并/远程） | 出现时先用一句话解释 |

---

## 五、常见疑问

**Q：我不会命令行，能用吗？**
A：完全不用。你只需要：说话、做选择、贴链接、点网页按钮。

**Q：换平台了怎么办？**
A：说"改成 GitHub"或"改成 GitLab"即可，一次生效。

**Q：一个电脑多个项目可以吗？**
A：可以。每个仓库各自操作，体检、提交、拉取、推送互不影响。

**Q：代码会传错地方吗？**
A：不会。推送前会确认远程地址；绑定远程地址需要你提供链接，助手从不乱猜。

---

*本插件让"git 小白"这个说法变成过去式。有问题、有建议，欢迎在仓库提 Issue。*
